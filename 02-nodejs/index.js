'use strict'

const express = require('express')

const User = require('./models/User')

// Setup Express.js app
const app = express()
    console.log(User);
// TODO: everything else

app.listen(3000)
